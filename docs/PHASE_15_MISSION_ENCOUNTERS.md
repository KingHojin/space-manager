# Phase 15 — Mission Encounter Cards

## Goal

Give contract missions their own choice-card moments instead of using only generic arrival events.

## PR A implemented

Files:

- `src/data/missionEncounters.js`
- `src/systems/missionEncounterSystem.js`
- `docs/PHASE_15_MISSION_ENCOUNTERS.md`

## Data

Added mission encounter templates for these mission categories:

- salvage
- rescue
- courier
- survey
- bounty
- mining
- research

Each template has:

- title
- category
- icon
- scene
- tags
- timing
- risk
- options

Each option has:

- label
- role
- risk
- rewardPreview
- outcomes

## System

Added pure helpers:

- `instantiateMissionEncounter`
- `generateMissionEncounter`
- `normalizeMissionEncounterRecord`
- `resolveMissionEncounterOption`
- `getMissionEncounterCandidates`

The matching score uses:

- mission tags
- mission encounterTags
- mission category
- requested timing
- mission risk

## PR B implemented

Added a reusable presentational component:

- `src/components/ui/MissionEncounterCard.jsx`

The card renders:

- encounter poster panel
- risk chip
- timing chip
- destination chip
- risk gauge
- scene text
- option cards
- option role chip
- option risk chip
- reward preview chips
- resource delta chips
- crew-risk indicator
- linked encounter indicator

PR B is UI-only. It accepts an `encounter` prop and optional `onSelectOption(optionId)` callback.

It does not read stores and does not connect to navigation, mission state, rewards, or existing gameplay flow.

## Scope

Current Phase 15 work is still foundation/UI only.

- Navigation behavior is not changed yet.
- Mission completion behavior is not changed yet.
- Payout is not applied yet.
- Existing formulas are not changed.

## Next PRs

Recommended sequence:

1. PR C: pending mission encounter state keyed by vessel or mission.
2. PR D: connect mission arrival in Exploration to mission encounter resolution.
3. PR E: apply option outputs through existing reward/resource/log systems.

## Local check

Connector cannot run local commands. Verify locally or in Codex:

```bash
npm install
npm run build
npm run dev
```

Manual checks:

1. Import `MissionEncounterCard` in a temporary dev harness.
2. Pass an encounter generated by `generateMissionEncounter`.
3. Confirm poster, scene, risk gauge, and option cards render.
4. Confirm option click calls `onSelectOption(optionId)`.
5. Confirm no gameplay behavior changes from PR B alone.
