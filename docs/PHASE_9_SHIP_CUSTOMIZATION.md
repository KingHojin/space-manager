# Phase 9 — Ship Customization

Deferred until after Phase 8 merge.

Implementation direction:

- Refactor Phase 5 room coefficients behind `getRoomModifiers`.
- Add room `tier` and `modules` state.
- Add module catalog and upgrade/install actions.
- Extend one-slot room assignment into multi-slot assignment.
- Feed `crisisResist` into Phase 6 crisis rolls.
- Add room upgrade UI.

This file is a marker so the next branch starts from the Phase 8 navigation baseline.
